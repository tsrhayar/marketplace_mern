import React from 'react'
import { Link } from "react-router-dom";

const Seller = () => {
    return (
        <div>
            <Link className="btn btn-primary" to="/addproduct">addproduct</Link>
        </div>
    )
}

export default Seller
